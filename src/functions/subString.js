// I think this is getting removed with 2.0

// export default (left, right) => {
//   let result = false;
//   if (typeof left !== 'undefined') {
//     result = left.some(param => (
//       right.indexOf(param) !== -1
//     ));
//   }

//   return result;
// };
